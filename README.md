# AIChat Pro Frontend

React + Vite frontend for the AIChat Pro app.

## Run

```bash
npm install
npm run dev
```

The frontend expects the ASP.NET Core backend at:

```env
VITE_API_URL=http://localhost:5000
```

Update `frontend/.env` if your backend runs on a different URL.
